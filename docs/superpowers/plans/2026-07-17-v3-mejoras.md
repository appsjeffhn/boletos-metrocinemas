# Boletos Metrocinemas v3 — Mejoras (plan reconciliado)

> **For agentic workers:** REQUIRED SUB-SKILL: usar superpowers:subagent-driven-development para ejecutar tarea por tarea. Pasos con checkbox `- [ ]`. Antes de ejecutar, resolver las **Decisiones pendientes** de abajo con el usuario.

**Goal:** Endurecer y profesionalizar la app existente: operación de taquilla robusta (offline, feedback, código manual), avisos de vencimiento en el dashboard, envío de QR por correo, confiabilidad (respaldos/monitoreo/papelera/notas), personalización del boleto imprimible, seguridad (cambio de contraseña + auditoría) y pulido de UI en lotes.

**Base:** Continúa sobre `master` (producción: https://boletos-metrocinemas.vercel.app). Stack: Next.js 16 (App Router, TS) · Drizzle · Neon Postgres / PGlite (tests) · Vitest · Tailwind v4 · Merlin design system · jose+bcrypt · qrcode · jszip · pdf-lib · PWA (manifest+SW).

---

## ⚠️ Estado reconciliado (actualizado 2026-07-18)

Este plan se redactó **antes** del rediseño Bento/Merlin, la reestructura de navegación y el pase de accesibilidad. Al reconciliarlo con el código actual:

| Tarea | Estado | Qué falta |
|-------|--------|-----------|
| A1 · Botones lote compactos + orden sedes | 🟡 PARCIAL | Botones compactos y en menú ⋯ **hechos** (rediseño). Falta **ordenar sedes alfabéticamente** en el selector de crear/editar lote. |
| B1 · Cambio/reseteo de contraseña | 🟡 PARCIAL | Reseteo por **admin ya existe** (editar usuario reescribe `passwordHash`). Falta **autoservicio `/cuenta`** (cambiar la propia con verificación de la actual). |
| B2 · Auditoría/bitácora | ⬜ PENDIENTE | Todo. Requiere **migración** (tabla `auditoria`). |
| C1 · Feedback canje (sonido/vibración) | ⬜ PENDIENTE | Todo. Sin migración. |
| C2 · Canje por código manual | ⬜ PENDIENTE | Todo. Sin migración. |
| C3 · Taquilla offline / cola PWA | ⬜ PENDIENTE | Todo (hoy solo hay PWA básica cache-first de assets). Requiere Decisión #3. |
| D1 · Banner de vencimientos en dashboard | ⬜ PENDIENTE | Todo. Sin migración. |
| E1 · Envío de QR por correo | ⬜ PENDIENTE | Todo. Requiere **migración** (`empresas.email`) + Decisión #1 (proveedor). |
| F1 · Papelera + motivo de anulación en reportes | 🟡 PARCIAL | Motivo de anulación **ya se ve en Lotes** (tooltip). Falta **mostrarlo en reportes** y la **papelera** (borrado suave, requiere migración `eliminadoEn`). |
| F2 · Monitoreo/health/respaldos | ⬜ PENDIENTE | Todo. Requiere Decisiones #2 y #4. |
| G1 · PDF imprimible personalizado | 🟡 PARCIAL | PDF actual = QR + código + leyenda "Metrocinemas". Falta **logo, nombre de empresa, fecha de vencimiento y leyenda de cortesía**. |
| G2 · Semáforo de vencimiento en lista de lotes | ✅ HECHO | `venceInfo` + clases `.semr/.sema/.semg` en `LotesPanel`. **Quitada de este plan.** |

**Reestructura de rutas (afecta todas las rutas del plan):** los paneles se movieron. Rutas actuales:
- Lotes: `src/app/(admin)/eventos/lotes/` · Productos: `src/app/(admin)/eventos/productos/` · Empresas: `src/app/(admin)/eventos/empresas/`
- Usuarios: `src/app/(admin)/configuracion/usuarios/` · Sedes: `src/app/(admin)/configuracion/sedes/` · Zona horaria: `src/app/(admin)/configuracion/zona-horaria/`
- Ya **no existen** `(admin)/lotes`, `(admin)/productos`, `(admin)/empresas`, `(admin)/usuarios`.
- El menú ⋯ de las colecciones es el componente compartido `src/components/RowMenu.tsx`.

**Migraciones agrupables:** B2 (`auditoria`), E1 (`empresas.email`), F1 (`eliminadoEn` en `empresas`/`lotes`) tocan `src/db/schema.ts`. Si se hacen juntas, conviene **una sola tanda de migración**.

## Contexto ya implementado (no rehacer)
Canje único atómico (`UPDATE ... WHERE estado='activo'`), restricción de canje por sedes del lote (`loteSedes`), lotes por empresa con prefijo (`M{prefijo}-XXXXXX`), roles combinables admin/taquilla + multi-sede con sede activa, dashboard (KPIs, canjes por sede, clientes activos, valores de items), reportes navegables + CSV, empresas/usuarios/lotes/productos/sedes CRUD, productos por lote (catálogo + bloqueo con canjes), editar/eliminar lote, anular con motivo, escaneo múltiple con código legible + "ver datos" de canjeados, descarga ZIP de QR + PDF, PWA instalable, **rediseño Bento/Merlin completo**, **colección tarjetas/tabla + filtros + menú ⋯**, **semáforo de vencimiento (G2)**, **accesibilidad de tablas/menús (RowMenu) + teclado numérico en DNI (DniInput)**, **reseteo de contraseña por admin**.

---

## Decisiones pendientes (resolver antes de ejecutar)
1. **Proveedor de correo** para envío de QR (recomendado: **Resend** vía Vercel Marketplace; alternativa: SMTP propio de Metrocinemas / Nodemailer). Requiere API key en variables de entorno. (Bloquea E1.)
2. **Monitoreo/errores:** **Vercel Observability + Log Drains** (nativo) y/o **Sentry**. Recomendado: empezar con Vercel nativo. (Bloquea parte de F2.)
3. **Alcance offline de taquilla:** aceptar explícitamente el límite anti-duplicidad (ver Task C3). (Bloquea C3.)
4. **Respaldos Neon:** confirmar plan/retención actual y si se quiere además un dump lógico programado. (Bloquea parte de F2.)

## Global Constraints
- UI en español, tema Merlin (tokens en `globals.css`).
- No romper el canje atómico ni la restricción por sede.
- `drizzle-orm/neon-http` NO soporta transacciones → seguir usando rollback compensatorio donde aplique.
- Fecha "hoy"/vencimiento en zona **America/Tegucigalpa** (helper `hoyISOEn` / `zonaHoraria`).
- Tests con `fileParallelism:false`.
- Autodeploy: push a `master` con git author `appjeffhn@gmail.com`.
- Flujo de trabajo: implementar en rama `sandbox` → preview Vercel apuntando a BD sandbox → aprobar → merge a `master`.

---

## FASE A — Ajustes rápidos de UI (bajo riesgo)

### Task A1: Orden alfabético de sedes en el modal de crear/editar lote  🟡 PARCIAL
**Files:** `src/app/(admin)/eventos/lotes/LotesPanel.tsx` (componente `SedesSelector`).
- Los botones de acción de lote ya son compactos y viven en el menú ⋯ (`RowMenu`) — **hecho por el rediseño, no rehacer**. La grilla de 2 columnas de sedes ya existe (`grid grid-cols-2 sm:grid-cols-3`).
- [ ] En `SedesSelector`, ordenar las sedes **alfabéticamente por nombre** (`localeCompare`, español) antes de renderizarlas, tanto en crear como en editar lote. Mantener "Todas las sucursales" arriba, separado.
- [ ] Verificar `tsc`+`build`. Commit.

---

## FASE B — Seguridad

### Task B1: Cambio de contraseña por autoservicio (`/cuenta`)  🟡 PARCIAL
**Files:** `src/app/cuenta/` (nueva página "Mi cuenta"), nuevo o existente en `src/domain/` para la lógica, `src/lib/auth.ts` (ya tiene `hashPassword`/`verifyPassword`), `src/components/AppShell.tsx` (link "Mi cuenta").
- El **reseteo por admin ya existe** (`src/app/(admin)/configuracion/usuarios/actions.ts` reescribe `passwordHash` al editar) — no rehacer, solo confirmar que sigue funcionando.
- [ ] **Cambio propio:** página `/cuenta` (cualquier usuario logueado) con formulario: contraseña actual + nueva + confirmar. Server action valida la actual con `verifyPassword`, exige mínimo 8 chars, hashea y actualiza. Link a "Mi cuenta" en el AppShell y en el header de taquilla.
- [ ] Tests de dominio (verifica actual, rechaza incorrecta, rechaza corta, actualiza hash). `tsc`+`build`+`test`. Commit.

### Task B2: Registro de auditoría (bitácora)  ⬜ PENDIENTE
**Files:** `src/db/schema.ts` (+ tabla `auditoria`), `src/test/db.ts`, `src/domain/auditoria.ts`, llamadas desde los server actions sensibles, `src/app/(admin)/configuracion/auditoria/page.tsx` (o bajo `(admin)/auditoria/`), migración.
- [ ] Tabla `auditoria`: `id`, `usuarioId`, `accion` (text: `crear_lote`, `anular_lote`, `editar_empresa`, `eliminar_empresa`, `crear_usuario`, `editar_usuario`, `reset_password`, `canje`, `canje_multiple`, …), `entidad` (ej. `lote:12`), `detalle` (jsonb/text), `creadoEn`. Índice por `creadoEn` y `usuarioId`.
- [ ] Helper `registrar(db, { usuarioId, accion, entidad, detalle })`. Llamarlo en cada acción sensible (empresas/usuarios/lotes/productos crear/editar/eliminar/anular, reset password). El canje ya deja registro en `boletos`; auditarlo es opcional.
- [ ] Página de auditoría (admin) con tabla filtrable por acción/usuario/fecha y **paginación**.
- [ ] Tests del helper + una acción. Migración `db:generate`. `tsc`+`build`+`test`. Commit.

---

## FASE C — Operación de taquilla

### Task C1: Feedback de canje (sonido + vibración)  ⬜ PENDIENTE
**Files:** `src/components/Scanner.tsx`, `src/app/canje/[token]/FormularioCanje.tsx`, `src/app/taquilla/multiple/MultiScanner.tsx`, nuevo `src/lib/feedback.ts` (client util).
- [ ] `feedback.ts`: `beepExito()` / `beepError()` con Web Audio API (tono corto agudo/grave) + `navigator.vibrate` (patrón corto éxito, doble error). Guardas para SSR (solo en cliente) y navegadores sin soporte.
- [ ] Disparar en: canje exitoso (verde) → éxito; rechazo (rojo) → error; en escaneo múltiple, un beep al agregar y al confirmar el resumen.
- [ ] `tsc`+`build`. Commit. (Verificación manual en dispositivo.)

### Task C2: Canje por código manual (QR dañado)  ⬜ PENDIENTE
**Files:** nuevo o existente en `src/domain/` (helper `buscarPorCodigo`), `src/app/taquilla/page.tsx`, `src/app/taquilla/multiple/*`, `src/app/canje/[token]/*`.
- [ ] Dominio: `buscarPorCodigo(db, codigo)` → devuelve el token (o info) de un boleto por su `codigo` legible (`M...-XXXXXX`), normalizando (mayúsculas, con/sin guion). El índice único en `codigo` ya existe.
- [ ] Taquilla individual: campo "Ingresar código" que, al enviar, resuelve el token y navega a `/canje/<token>`.
- [ ] Escaneo múltiple: permitir **agregar por código** además de escanear (input + botón "Agregar código").
- [ ] Tests de `buscarPorCodigo`. `tsc`+`build`+`test`. Commit.

### Task C3: Taquilla offline / tolerancia a mala señal (PWA)  ⬜ PENDIENTE
> **IMPORTANTE (garantía anti-duplicidad):** el canje offline NO puede garantizar unicidad en el momento (dos dispositivos sin red podrían "canjear" el mismo boleto). Diseño: **validación cacheada + cola de canjes con reconciliación en el servidor** (fuente de verdad). Al reconectar, se envían los canjes encolados; si el servidor detecta que ya estaba canjeado, ese canje offline se marca **fallido/duplicado** y se alerta al operador. Confirmar (Decisión #3).

**Files:** `public/sw.js` (ampliar — hoy es cache-first mínimo de assets, no toca `/api/`), `src/lib/offlineQueue.ts` (IndexedDB), `src/components/Scanner.tsx`/`MultiScanner.tsx`, `src/app/canje/[token]/actions.ts`, nuevo endpoint `src/app/api/canje/sync/route.ts`, indicador de conexión en la UI de taquilla.
- [ ] Service worker: cachear el "app shell" de taquilla (rutas + assets) para que cargue sin red (cache-first para estáticos; nunca cachear respuestas de canje/auth).
- [ ] `offlineQueue.ts`: cola en **IndexedDB** de canjes pendientes `{ token, portadorNombre, portadorDni, sedeId, ts }`. Si el POST de canje falla por red, encolar y mostrar "Guardado sin conexión, se sincronizará".
- [ ] Sincronización: al reconectar (evento `online` y/o Background Sync), enviar la cola a `/api/canje/sync` que ejecuta `canjearBoleto` por cada uno (misma guardia atómica) y devuelve resultado por token; actualizar UI y **alertar los duplicados/fallidos**.
- [ ] Indicador de estado (En línea / Sin conexión + N pendientes) en el header de taquilla.
- [ ] Tests del endpoint sync + de la lógica de cola (extraer lógica pura). `tsc`+`build`+`test`. Commit. Verificación manual con red desconectada.

---

## FASE D — Dashboard: avisos de vencimiento

### Task D1: Banner de vencimientos en el dashboard  ⬜ PENDIENTE
**Files:** `src/domain/dashboard.ts` (o `src/domain/lotesQuery.ts`), `src/app/(admin)/dashboard/page.tsx`, componente `src/components/AvisoVencimientos.tsx`.
- [ ] Query `lotesPorVencer(db)` → lotes NO anulados con boletos **pendientes** cuyo `fechaVencimiento` esté: (a) **vencido** (< hoy) con pendientes, y (b) **por vencer** (dentro de los próximos N días, ej. 7). Devolver conteos + lista. Reusar la lógica de días de `venceInfo` si aplica.
- [ ] En el dashboard, **banner tipo alerta** (tokens warning para "por vencer", error para "vencidos") arriba: ej. "3 lotes vencen esta semana · 1 lote vencido con boletos sin canjear", con **botón "Ver lotes"** a `/eventos/lotes`. Descartable por sesión (opcional).
- [ ] Tests de `lotesPorVencer` (America/Tegucigalpa). `tsc`+`build`+`test`. Commit.

---

## FASE E — Envío de QR al cliente por correo

### Task E1: Correo con los QR del lote  ⬜ PENDIENTE (requiere Decisión #1 + migración)
> Recomendado **Resend**.
**Files:** `src/db/schema.ts` (+ `empresas.email`), migración, `src/lib/mailer.ts`, `src/app/(admin)/eventos/lotes/actions.ts` (acción `enviarQrPorCorreo`), UI en lotes/empresas (`src/app/(admin)/eventos/empresas/EmpresasTabla.tsx`).
- [ ] Agregar `empresas.email` (nullable) + editarlo en la pantalla de Empresas.
- [ ] `mailer.ts`: wrapper del proveedor (Resend API key en env). `enviarCorreo({ to, subject, html, attachments })`.
- [ ] Acción `enviarQrPorCorreo(loteId)` (admin): genera el **PDF del lote** (reusar `src/app/api/lote/[id]/pdf/route.ts` o su función interna) y/o el ZIP, y lo envía como adjunto al `email` de la empresa, con cuerpo HTML de marca. Registrar en auditoría (si B2 está). Email faltante → mensaje claro.
- [ ] Botón "Enviar por correo" en el menú ⋯ del lote (confirma destinatario). `tsc`+`build`. Commit. Verificación con correo real.

---

## FASE F — Confiabilidad

### Task F1: Papelera (borrado suave) + motivo de anulación en reportes  🟡 PARCIAL
**Files:** `src/db/schema.ts` (+ `eliminadoEn` en `empresas` y `lotes`), migración, dominio (filtrar eliminados), actions de eventos, vista de papelera, `src/app/(admin)/reportes/[empresaId]/page.tsx`.
- El **motivo de anulación ya se muestra en Lotes** (tooltip `title` en tarjetas y tabla) — no rehacer.
- [ ] Cambiar borrado duro de **empresas** y **lotes** por **borrado suave** (`eliminadoEn timestamp`), y excluir eliminados de todas las listas/queries por defecto. Mantener la guarda (no eliminar con canjes/activos según reglas actuales).
- [ ] Vista **"Papelera"** (admin) para restaurar o eliminar definitivamente (con confirmación).
- [ ] Mostrar en **reportes** (`reportes/[empresaId]/page.tsx`, hoy solo pinta el badge "Anulado" sin motivo) el **motivo de anulación** y fecha/usuario.
- [ ] Tests de filtrado (eliminados no aparecen) + restaurar. Migración. `tsc`+`build`+`test`. Commit.

### Task F2: Monitoreo, alertas y respaldos (infra/documentación)  ⬜ PENDIENTE (Decisiones #2 y #4)
**Files:** `docs/OPERACION.md` (carpeta `docs/` de operación aún no existe), opcional `sentry` config, `src/app/api/health/route.ts`.
- [ ] Endpoint `/api/health` (chequea conexión a BD) para monitoreo externo (uptime).
- [ ] Configurar **alertas de errores** en producción (Vercel Observability / Log Drains; opcional Sentry con `@sentry/nextjs`). Documentar cómo se revisan.
- [ ] **Respaldos:** documentar/verificar retención de Neon (branching/point-in-time) y, si se decide, un **cron** (`vercel.ts` crons o Neon) que haga un dump lógico periódico a Blob. Documentar restauración.
- [ ] Documentar en `docs/OPERACION.md`. Commit.

---

## FASE G — Producto/UX

### Task G1: Boleto/QR imprimible personalizado  🟡 PARCIAL
**Files:** `src/app/api/lote/[id]/pdf/route.ts` (mejorar), assets (logo PNG).
- Hoy el PDF genera grilla 3×4 (Letter) con, por boleto: QR (`/canje/{token}`) + código en negrita + leyenda fija "Metrocinemas". Falta lo demás.
- [ ] Mejorar el PDF: incluir por boleto/encabezado el **logo de Metrocinemas**, **nombre de la empresa**, **código**, **fecha de vencimiento**, y una leyenda ("Cortesía · válido hasta …"). Embeber el logo (PNG) con pdf-lib.
- [ ] Opcional: pequeña personalización (texto/leyenda por lote). `tsc`+`build`. Commit. Verificación abriendo el PDF.

### ~~Task G2: Semáforo de vencimiento~~  ✅ HECHO — retirada del plan
`venceInfo` + `.semr/.sema/.semg` ya en `src/app/(admin)/eventos/lotes/LotesPanel.tsx` (tarjetas y tabla), basado en America/Tegucigalpa.

---

## Orden sugerido de ejecución (revisado)

**Ganancias rápidas sin migración (recomendado empezar aquí):**
1. **A1** — ordenar sedes alfabéticamente (muy pequeño).
2. **C1** — feedback sonido/vibración en taquilla.
3. **C2** — canje por código manual.
4. **D1** — banner de vencimientos en dashboard.
5. **G1** — PDF personalizado (logo/empresa/vencimiento/leyenda).
6. **B1** — autoservicio `/cuenta` de cambio de contraseña.

**Con migración de BD (agrupar en una tanda):**
7. **B2** — auditoría (tabla `auditoria`).
8. **F1** — papelera (`eliminadoEn`) + motivo en reportes.
9. **E1** — correo (`empresas.email`, tras Decisión #1).

**Infra / mayor complejidad (al final):**
10. **F2** — health/monitoreo/respaldos (Decisiones #2 y #4).
11. **C3** — taquilla offline con cola (Decisión #3; el más complejo).

## Self-Review (cobertura del pedido original)
- Taquilla: C1 sonido/vibración ⬜, C2 código manual ⬜, C3 offline ⬜.
- Vencimiento: banner dashboard D1 ⬜; semáforo en lista G2 ✅.
- Envío de QR al cliente: E1 ⬜ (migración + proveedor).
- Confiabilidad: F1 papelera ⬜ / motivo en Lotes ✅ / motivo en reportes ⬜; F2 monitoreo+respaldos ⬜.
- Producto/UX: G1 boleto personalizado 🟡; G2 semáforo ✅.
- Seguridad: B1 cambio propio ⬜ / reseteo admin ✅; B2 auditoría ⬜.
- Botones de lotes compactos ✅ (rediseño); orden de sedes A1 ⬜.
